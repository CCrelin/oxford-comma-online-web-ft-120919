def oxford_comma(array)
  while array.length == array.length
    return array.join("")
  while array.length == array.length
    return array.join(" and ")
  while array.length == array.length
    array.join(", ")
end
end
end 
end